# parse_job prompt (Step 2)

Used with the Haiku model. Filled in during Step 2.

Inputs:
- `{cleaned_html_text}`
- `{job_url}`

Required output: a JSON object matching the `ParsedJob` Pydantic schema in `src/lib/models.py`.
